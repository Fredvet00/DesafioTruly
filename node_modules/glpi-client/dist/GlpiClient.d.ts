import { IAuthParams } from './interfaces/IAuthParams';
import { GlpiAPI } from './GlpiAPI';
export declare class GlpiClient {
    private url;
    private httpDefaults;
    constructor(url: string);
    initSession(auth: IAuthParams, relogin?: boolean): Promise<GlpiAPI>;
    private ajustRelogin;
}
