import { AxiosError } from 'axios';
export declare class GlpiResponseException extends Error {
    statusCode: number;
    statusText: string;
    reason: string;
    code: string;
    constructor(error: AxiosError);
}
